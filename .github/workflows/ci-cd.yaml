name: CI-Pipeline

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - main
  # workflow_dispatch:

jobs:

  ci-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint GitHub Actions workflow
        uses: raven-actions/actionlint@v2

  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      ml: ${{ steps.filter.outputs.ml }}
      container: ${{ steps.filter.outputs.container }}
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: detect changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'tests/api/**'
              - 'tests/conftest.py'
              - 'pyproject.toml'
              - 'uv.lock'
            ml:
              - 'src/**'
              - 'data/**'
              - 'dvc.yaml'
              - 'dvc.lock'
              - 'params.yaml'
              - 'Prod-Script/**'
              - 'tests/model/**'
              - 'pyproject.toml'
              - 'uv.lock'

  ml-pipeline:
    needs: changes
    if: (github.event_name == 'push') && (github.ref == 'refs/heads/main') && (needs.changes.outputs.ml == 'true')
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.4"

      - name: install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 0.11.6

      - name: cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: install dependencies
        run: uv sync --extra test --extra pipeline

      - name: pull dvc data
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_DVC_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_DVC_SECRET_ACCESS_KEY }}
        run: uv run dvc pull data/raw/urbaneats-dataset.csv

      - name: run dvc pipeline
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
        run: uv run dvc repro

      - name: run model tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
        run: uv run pytest tests/model -q

  model-tests:
    needs:
      - changes
    if: |
      always() &&
      github.event_name == 'pull_request' &&
      needs.changes.outputs.ml == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.4"

      - name: install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 0.11.6

      - name: cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: install dependencies
        run: uv sync --extra test --extra pipeline

      - name: run model tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
        run: uv run pytest tests/model -q


  api-tests:
    needs:
      - changes
    if: |
      needs.changes.outputs.backend == 'true'
      
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.4"

      - name: install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 0.11.6

      - name: cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: install dependencies
        run: uv sync --extra test

      - name: run unit tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
          API_KEY: ${{ secrets.API_KEY }}
          JWT_EXPIRY_MINUTES: ${{ secrets.JWT_EXPIRY_MINUTES }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
          SERVICE_ID: ${{ secrets.SERVICE_ID }}
          JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
        run: uv run pytest tests/api/unit -q

      - name: run integration tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
          API_KEY: ${{ secrets.API_KEY }}
          JWT_EXPIRY_MINUTES: ${{ secrets.JWT_EXPIRY_MINUTES }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
          SERVICE_ID: ${{ secrets.SERVICE_ID }}
          JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
        run: uv run pytest tests/api/integration -q

      - name: upload api test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: api-test-logs
          path: logs/

  promote-model:
    needs:
      - changes
      - ml-pipeline
    if: (github.event_name == 'push') && (github.ref == 'refs/heads/main') && (needs.changes.outputs.ml == 'true') && (needs.ml-pipeline.result == 'success')
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.4"

      - name: install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 0.11.6

      - name: cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: install dependencies
        run: uv sync --extra test

      - name: promote model
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          PYTHONPATH: ${{ github.workspace }}
        run: uv run python Prod-Script/promote_model.py

  post-promotion-api-checks:
    needs:
      - changes
      - promote-model
    if: (github.event_name == 'push') && (github.ref == 'refs/heads/main') && (needs.changes.outputs.ml == 'true' && needs.promote-model.result == 'success')
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11.4"

      - name: install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 0.11.6

      - name: cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: install dependencies
        run: uv sync --extra test

      - name: run post-promotion unit tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
          API_KEY: ${{ secrets.API_KEY }}
          JWT_EXPIRY_MINUTES: ${{ secrets.JWT_EXPIRY_MINUTES }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
          SERVICE_ID: ${{ secrets.SERVICE_ID }}
          JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
        run: uv run pytest tests/api/unit -q

      - name: run post-promotion integration tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_PAT }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_PAT }}
          API_KEY: ${{ secrets.API_KEY }}
          JWT_EXPIRY_MINUTES: ${{ secrets.JWT_EXPIRY_MINUTES }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
          SERVICE_ID: ${{ secrets.SERVICE_ID }}
          JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
        run: uv run pytest tests/api/integration -q

      - name: upload post-promotion api logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-promotion-api-logs
          path: logs/